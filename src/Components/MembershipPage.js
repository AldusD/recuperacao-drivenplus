import { useState, useEffect, useContext } from "react";
import axios from "axios";

import UserContext from "../contexts/UserContext";
export default function MembershipPage() {
    // Logic
    useEffect(() => {
        // const promise = axios.get()
    }, [])

    // UI
    return (
        <h1>xboscbovdsvdsvdsvd</h1>
    );
}